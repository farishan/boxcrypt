const UI = {
  map: el('map'),
  exits: el('exits'),
  player: el('player'),
  currentRoom: el('currentRoom'),
  actions: el('actions'),
  boxprompt: el('boxprompt'),
}

// Base Styling
UI.map.className += "border p-2 inline-block"